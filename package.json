{
  "name": "vinayak-foods",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "npm run dev --prefix my-app",
    "build": "npm run build --prefix my-app",
    "start": "npm run start --prefix my-app"
  }
}
